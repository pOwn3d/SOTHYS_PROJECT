{% extends 'base.html.twig' %}

{% block title %}Coedi Sothys - Vérifier vos email{% endblock %}

{% block body %}

    <main class="main" id="top">
        <div class="container">
            <div class="row flex-center min-vh-100 py-6">
                <div class="col-sm-10 col-md-8 col-lg-6 col-xl-5 col-xxl-4"><a class="d-flex flex-center mb-4" href="{{ path('app_login') }}"><img
                                src="{{ asset('assets/images/logo_register.png') }}"/></a>
                    <div class="card">
                        <div class="card-body p-4 p-sm-5">
                            <div class="text-center"><img class="d-block mx-auto mb-4" src="{{ asset('assets/images/envelope.png') }}" alt="Email" width="70" />
                                <h4> Veuillez vérifier votre messagerie! </h4>
                                <p> Un e-mail a été envoyé. Veuillez cliquer sur le lien inclus pour réinitialiser votre mot de passe.
                                </p>
                                <p>Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p><a class="btn btn-primary btn-sm mt-3" href="{{ path('app_login') }}"><span class="fas fa-chevron-left mr-1" data-fa-transform="shrink-4 down-1"></span>Revenir à la connexion</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>


{% endblock %}
